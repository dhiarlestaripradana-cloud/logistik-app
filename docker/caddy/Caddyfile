# HTTPS otomatis via Let's Encrypt. Ganti {$APP_DOMAIN} lewat env di compose.
{$APP_DOMAIN} {
    encode gzip zstd
    reverse_proxy app:3000
    # Batasi ukuran upload foto bukti (sudah dikompresi klien < 300KB, beri buffer)
    request_body {
        max_size 10MB
    }
}
